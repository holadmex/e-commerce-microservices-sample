apiVersion: v1
kind: Service
metadata:
  name: {{ include "cart-cna-microservice.fullname" . }}
  labels:
    {{- include "cart-cna-microservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cart-cna-microservice.selectorLabels" . | nindent 4 }}
